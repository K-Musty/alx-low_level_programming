#include "main.h"
/**
 * _abs -computes the absolute value integer
 * @n: the int to be computed
 *
 * Return: the sbsolut value to the int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
