#include "main.h"
/**
 * mul - This function will multiply two numbers
 * @a: The first integer to multipy
 * @b: second integer to multiply
 * Return: the result
 */
int mul(int a, int b)
{
	return (a * b);
}
