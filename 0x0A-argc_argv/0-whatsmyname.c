#include "main.h"
#include <stdio.h>

/**
 * main - main method
 * @argc: number of arguments
 * @argv: pointers to arguments
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	if (argc)
		printf("%s\n", argv[0]);
	return (0);
}
